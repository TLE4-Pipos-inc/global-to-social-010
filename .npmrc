# Don't re-check / re-install dependencies before running a script.
# In Docker builds, node_modules is installed in a dedicated stage and copied
# in, so pnpm's pre-run deps check would otherwise trigger a full (networked)
# reinstall on every `pnpm run`, which hangs the build and exhausts the host.
verify-deps-before-run=false
